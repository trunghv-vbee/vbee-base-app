/**
 * @flow
 */

import en from 'assets/languages/locales/en';
import vi from 'assets/languages/locales/vi';
import LocalizedStrings from 'react-native-localization';

export default new LocalizedStrings({
  // en,
  vi,
});
